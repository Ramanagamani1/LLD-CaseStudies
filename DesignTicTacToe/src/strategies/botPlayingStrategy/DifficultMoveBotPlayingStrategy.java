package strategies.botPlayingStrategy;

import models.*;

public class DifficultMoveBotPlayingStrategy implements BotPlayingStrategy {

    @Override
    public Move makeNextMove(Board board, Player player) {
        return null;
    }
}
